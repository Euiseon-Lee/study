import java.io.BufferedReader;
import java.io.InputStreamReader;
import java.io.IOException;

public class Main {
    public static void main(String[] args) throws IOException {
        /**
         * 🐱 BOJ 27961 고양이는 많을수록 좋다 – 역방향 축소(ceil(n/2))로 최소 행동 수 산출
         *
         * [문제 핵심]
         * - 시작 0마리에서 정확히 N마리를 만드는 최소 행동 수.
         * - 한 번의 행동:
         *   (1) 1마리 생성(+1)
         *   (2) 현재 k마리에서 0~k마리 "추가"(부분 복제 가능) ⇒ 한 번에 k→[k..2k] 도달 가능
         *
         * [아이디어]
         * - 정방향 대신 역방향으로 N에서 줄이기:
         *   • 복제의 역연산: n → ceil(n/2) (짝수는 n/2, 홀수는 (n-1)/2)
         *   • 매 라운드 n = ceil(n/2), 카운트 cat++
         *   • n ≤ 3이 되면 복제보다 생성이 유리하므로, 생성 n번으로 종료
         * - 총 행동 수 = cat(복제 역추적 횟수) + n(마지막 생성 횟수)
         *
         * [정확성 근거]
         * - k→[k..2k] 범위에 한 번에 도달 가능하므로 역방향 축소가 ceil(n/2)로 정의됨.
         * - ceil-반감(cat회) 후 n≤3 구간에서는 복제 1회 + 생성 ⩾ 생성 n회이므로 생성만이 최적.
         * - 본 방식은 수식적으로  N≥1에 대해 1 + ceil(log2(N))과 동치(N=0이면 0).
         *
         * [복잡도/구현]
         * - 시간 O(log N), 공간 O(1)
         * - 구현: while (n > 3) { cat++; n = (n/2) + (n%2); }  // n ← ceil(n/2)
         *         answer = cat + n  // 마지막 생성 n번
         *
         * [보완 포인트]
         * - N=0일 때 0을 즉시 출력(명시적 경계 처리)
         * - n = (n >> 1) + (n & 1)로 ceil(n/2) 분기 없는 계산 가능(가독성 취향)
         * - 대안(참고): O(1) 공식 1 + ceil(log2(N))를 비트 길이로 계산
         */
        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
        long n = Long.parseLong(br.readLine());

        if (n == 0) { // 경계값 명시
            System.out.println(0);
            return;
        }

        long steps = 0;
        while (n > 3) {
            steps++;
            n = (n >> 1) + (n & 1); // ceil(n/2)
        }
        System.out.println(steps + n); // 마지막 구간은 생성 n번이 최적
    }
}

