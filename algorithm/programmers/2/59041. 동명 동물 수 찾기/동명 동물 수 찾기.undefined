-- 코드를 입력하세요
SELECT T1.NAME
     , T1.COUNT
  FROM (
        SELECT NAME
             , COUNT(NAME) AS COUNT
          FROM ANIMAL_INS 
         WHERE NAME IS NOT NULL
         GROUP BY NAME
         ORDER BY NAME
        ) AS T1
 WHERE T1.COUNT > 1;